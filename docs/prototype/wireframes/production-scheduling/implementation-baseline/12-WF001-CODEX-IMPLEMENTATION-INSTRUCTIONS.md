# WF-001 — Codex Instructions
Antes de codificar: ler manifestos, localizar arquitetura frontend, reutilizar componentes, preservar módulos existentes, usar rota demo isolada e registrar divergências de domínio.

Componentes conceituais: DemoShell, DemoNavigation, DemoScenarioState, ProductionSchedulingPage, HourByHourSchedule, ScheduleSummary, DataFreshness, BufferCoverageSummary, OperationalAttentionSummary, LotDetail, ProductionOrderCorrelation. Adaptar nomes às convenções reais.

Usar fixtures e estado frontend. Preferir `/demo` ou namespace existente.

Não criar backend, integração Balancing/PyMAC, banco, APIs corporativas, event contracts, Resource assignment inventado, execução ou persistência oficial.

Se imagem conflitar com manifesto, manifesto vence.

Não reproduzir da imagem qualquer indicação de “próxima atualização automática” enquanto frequência/SLA permanecer sem regra aprovada. Preservar apenas fonte, data da informação, última recepção/atualização e estado de atualidade/defasagem.
