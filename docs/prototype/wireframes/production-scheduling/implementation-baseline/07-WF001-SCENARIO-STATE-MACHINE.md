# WF-001 — Demo State
Estado local mínimo: selectedArea, selectedDateOffset, selectedDestination, selectedLot, activeScheduleVersion, comparisonVersion, schedules, productionOrders, bufferPositions, dataFreshness.

Cenários: normal; schedule stale; Balancing×PyMAC divergence; buffer risk; raw-material attention; new schedule version; selected Lot.

Ações do WF-001 não alteram lifecycle de execução. Nenhum Lot pode tornar-se ACTIVE nesta tela.
